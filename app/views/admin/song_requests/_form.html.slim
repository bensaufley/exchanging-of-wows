= form_for @song_request, html: { id: 'song-request-form', autocomplete: 'off' } do |song_request_form|
  .row
    .large-4.columns= label_tag 'search', 'Search', class: 'middle'
    .large-8.columns
      .relative= text_field_tag 'search', '', placeholder: 'Song Name'
  = song_request_form.hidden_field :name, required: true
  = song_request_form.hidden_field :artist, required: true
  = song_request_form.hidden_field :album, required: true
  = song_request_form.hidden_field :info, required: true
  .row
    .large-8.large-offset-4.columns = song_request_form.button 'Request', type: :submit, class: 'button expanded'
